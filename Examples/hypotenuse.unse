// Calculate the hypotenuse of a right angle triangle
// Compute sqrt(a^2 + b^2) per microstate

let idx = lift1(index, const(0))
let a = lift1(sin, idx)
let b = lift1(cos, idx)
let hypSquared = (a *u a) +u (b *u b)
let hyp = sqrtU(hypSquared)
plotU(hyp)